---
description: Remove this session's display client routing. Future events fall back to the firmware's default client slot.
---

The route has been removed by the plugin's hook. Run this command to confirm and report the current state to the user:

```bash
python3 "${CLAUDE_PLUGIN_ROOT}/bin/pin.py" status
```

One line is enough.
