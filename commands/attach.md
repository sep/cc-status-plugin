---
description: Pin the claude-status transport to this session so it won't auto-switch to newer Claude sessions.
---

The pin has been applied by the plugin's hook. Run this command to confirm and report the current state to the user:

```bash
"${CLAUDE_PLUGIN_ROOT}/bin/pin.py" status
```

One line about what's pinned is enough.
