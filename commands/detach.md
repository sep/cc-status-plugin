---
description: Release the claude-status transport pin. Auto-switching to the newest session resumes.
---

Run this command and report its output tersely to the user. One line is enough.

```bash
python3 "${CLAUDE_PLUGIN_ROOT}/bin/pin.py" detach
```
